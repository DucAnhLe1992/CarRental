- [x] Verify that the copilot-instructions.md file in the .github directory is created.
- [x] Clarify Project Requirements.
- [x] Scaffold the Project.
- [x] Customize the Project.
- [x] Install Required Extensions (none required).
- [x] Compile the Project.
- [x] Create and Run Task.
- [ ] Launch the Project (debug launch pending user confirmation).
- [x] Ensure Documentation is Complete.

## Completion Summary
- Built a Node.js + Express backend in TypeScript with ES modules in the current workspace.
- Split the app into route, controller, service, validation, data, and type modules.
- Implemented `GET /cars`, `GET /cars/:id`, `POST /cars`, `PUT /cars/:id`, and `DELETE /cars/:id`.
- Added status-code handling for `200`, `201`, `400`, and `404`.
- Added in-memory car dataset and payload validation.
- Added `README.md` with setup and Postman testing steps.
- Added VS Code task `Run Rental API` to start the backend in dev mode.
